diff --git a/packages/opencode/src/config/config.ts b/packages/opencode/src/config/config.ts
index 8236b30..dad9d30 100644
--- a/packages/opencode/src/config/config.ts
+++ b/packages/opencode/src/config/config.ts
@@ -424,11 +424,19 @@ const InfoSchema = Schema.Struct({
         description:
           "Predict the user's likely next prompt after each turn and show it as inline ghost text (Tab to accept). Enabled by default; set to false to disable.",
       }),
+      judgeModel: Schema.optional(Schema.String).annotate({
+        description:
+          'Independent model for LLM-as-judge phases ("provider/model"): the goal stop-condition gate, and the max-mode judge unless maxMode.judgeModel overrides it. Blind verification — the working model must not approve its own work. Falls back to the session/step model when unset or unresolvable.',
+      }),
       maxMode: Schema.optional(
         Schema.Struct({
           candidates: Schema.optional(PositiveInt).annotate({
             description: "Number of parallel reasoning candidates per step in max mode (default 5).",
           }),
+          judgeModel: Schema.optional(Schema.String).annotate({
+            description:
+              'Separate model for the judge phase ("provider/model"). Blind verification: keeps the generator from approving its own work. Overrides experimental.judgeModel for max mode; falls back to the step model when unset or unresolvable.',
+          }),
         }),
       ).annotate({
         description:
diff --git a/packages/opencode/src/session/goal.ts b/packages/opencode/src/session/goal.ts
index 4bba0e3..1cdb0fe 100644
--- a/packages/opencode/src/session/goal.ts
+++ b/packages/opencode/src/session/goal.ts
@@ -1,4 +1,4 @@
-import { Effect, Layer, Context, Option } from "effect"
+import { Effect, Layer, Context, Option, Exit, Cause } from "effect"
 import { generateObject, streamObject, type ModelMessage } from "ai"
 import z from "zod"
 import * as OtelTracer from "@effect/opentelemetry/Tracer"
@@ -147,14 +147,37 @@ export const layer = Layer.effect(
       model: { providerID: ProviderID; modelID: ModelID }
     }) {
       const cfg = yield* config.get()
-      const resolved = yield* provider.getModel(input.model.providerID, input.model.modelID)
+      // Blind verification: prefer the configured INDEPENDENT judge model so
+      // the working model can't approve its own stop. Unresolvable override →
+      // loud warn + fall back to the session model (old behavior), never a
+      // silent behavior change.
+      const judgeOverride = cfg.experimental?.judgeModel
+      const judgeExit = judgeOverride
+        ? yield* provider
+            .getModel(
+              Provider.parseModel(judgeOverride).providerID,
+              Provider.parseModel(judgeOverride).modelID,
+            )
+            .pipe(Effect.exit)
+        : undefined
+      if (judgeExit && Exit.isFailure(judgeExit))
+        yield* elog.warn("goal judge model not found; falling back to session model", {
+          judgeModel: judgeOverride,
+          error: String(Cause.squash(judgeExit.cause)),
+        })
+      const judgeRef =
+        judgeExit && Exit.isSuccess(judgeExit) ? Provider.parseModel(judgeOverride!) : input.model
+      const resolved =
+        judgeExit && Exit.isSuccess(judgeExit)
+          ? judgeExit.value
+          : yield* provider.getModel(input.model.providerID, input.model.modelID)
       const language = yield* provider.getLanguage(resolved)
       const tracer = cfg.experimental?.openTelemetry
         ? Option.getOrUndefined(yield* Effect.serviceOption(OtelTracer.OtelTracer))
         : undefined
 
-      const authInfo = yield* auth.get(input.model.providerID).pipe(Effect.orDie)
-      const isOpenaiOauth = input.model.providerID === "openai" && authInfo?.type === "oauth"
+      const authInfo = yield* auth.get(judgeRef.providerID).pipe(Effect.orDie)
+      const isOpenaiOauth = judgeRef.providerID === "openai" && authInfo?.type === "oauth"
 
       // Convert the conversation to native model messages so the judge sees the
       // real tool calls/results/images — same context the working agent had.
diff --git a/packages/opencode/src/session/max-mode.ts b/packages/opencode/src/session/max-mode.ts
index f9c885e..2d05dfd 100644
--- a/packages/opencode/src/session/max-mode.ts
+++ b/packages/opencode/src/session/max-mode.ts
@@ -68,6 +68,11 @@ export type MaxStepInput = {
   toolChoice?: "auto" | "required" | "none"
   /** Number of parallel candidates (default 5). */
   candidates?: number
+  /**
+   * Blind verification: separate model for the judge call, so the generator
+   * model can't approve its own work. Falls back to `model` when unset.
+   */
+  judgeModel?: Provider.Model
   /**
    * Optional hook to surface progress to the UI during the (otherwise
    * invisible) candidate + judge phases. Called with a short English label,
@@ -270,7 +275,9 @@ export const judge = (input: MaxStepInput, candidates: Candidate[]): Effect.Effe
       user: input.user,
       sessionID: input.sessionID,
       parentSessionID: input.parentSessionID,
-      model: input.model,
+      // Blind verification: an independent judge model when configured —
+      // the generator model must not approve its own candidates.
+      model: input.judgeModel ?? input.model,
       agent: input.agent,
       permission: input.permission,
       system: [JUDGE_SYSTEM],
diff --git a/packages/opencode/src/session/prompt.ts b/packages/opencode/src/session/prompt.ts
index 0209956..c18e49a 100644
--- a/packages/opencode/src/session/prompt.ts
+++ b/packages/opencode/src/session/prompt.ts
@@ -3878,9 +3878,26 @@ NOTE: At any point in time through this workflow you should feel free to ask the
                 Effect.provideService(ToolRegistry.Service, registry),
               )
             lastSystemPrompt = prebuiltSystem
-            const maxModeCfg = (yield* config.get()).experimental?.maxMode
+            const expCfg = (yield* config.get()).experimental
+            const maxModeCfg = expCfg?.maxMode
             const useMaxMode =
               agent.name === MaxMode.MAX_MODE_AGENT && maxModeCfg !== undefined && format.type !== "json_schema"
+            // Blind verification: judge may run on a SEPARATE model so the
+            // generator can't approve its own work. maxMode.judgeModel wins,
+            // then the general experimental.judgeModel. Unresolvable → loud warn +
+            // fall back to the step model (never a silent behavior change).
+            const judgeModelRef = maxModeCfg?.judgeModel ?? expCfg?.judgeModel
+            const judgeModel = yield* Effect.gen(function* () {
+              if (!useMaxMode || !judgeModelRef) return undefined
+              const parsed = Provider.parseModel(judgeModelRef)
+              const exit = yield* provider.getModel(parsed.providerID, parsed.modelID).pipe(Effect.exit)
+              if (Exit.isSuccess(exit)) return exit.value
+              yield* slog.warn("max-mode judge model not found; falling back to step model", {
+                judgeModel: judgeModelRef,
+                error: String(Cause.squash(exit.cause)),
+              })
+              return undefined
+            })
 
             const processArgs = {
               user: lastUser,
@@ -3947,6 +3964,7 @@ NOTE: At any point in time through this workflow you should feel free to ask the
                   handle,
                   llm,
                   candidates: maxModeCfg?.candidates,
+                  judgeModel,
                   setStatus: (message) =>
                     status.set(sessionID, message ? { type: "busy", message } : { type: "busy" }),
                 })
diff --git a/packages/opencode/test/session/max-mode-judge-model.test.ts b/packages/opencode/test/session/max-mode-judge-model.test.ts
new file mode 100644
index 0000000..3c9483c
--- /dev/null
+++ b/packages/opencode/test/session/max-mode-judge-model.test.ts
@@ -0,0 +1,74 @@
+import { describe, expect, test } from "bun:test"
+import { Effect } from "effect"
+import * as Stream from "effect/Stream"
+import { judge, type Candidate, type MaxStepInput } from "../../src/session/max-mode"
+import type { LLM } from "../../src/session/llm"
+
+/**
+ * Blind verification: when `judgeModel` is set, the judge call must go to that
+ * SEPARATE model — the generator model must not approve its own candidates.
+ * When unset, behavior is unchanged (falls back to the step model).
+ */
+
+/** Mock LLM that records which model each stream call was given. */
+function captureLLM() {
+  const models: unknown[] = []
+  const llm = {
+    buildSystemArray: () => Effect.succeed([]),
+    stream: (input: { model: unknown }): Stream.Stream<LLM.Event, unknown> => {
+      models.push(input.model)
+      return Stream.fromIterable([
+        { type: "text-delta", text: "1" } as LLM.Event,
+        { type: "finish-step", finishReason: "stop", usage: { inputTokens: 1, outputTokens: 1 } } as LLM.Event,
+      ])
+    },
+  } as unknown as LLM.Interface
+  return { llm, models }
+}
+
+const GENERATOR_MODEL = { info: { id: "generator" } }
+const JUDGE_MODEL = { info: { id: "independent-judge" } }
+
+function input(llm: LLM.Interface, judgeModel?: unknown): MaxStepInput {
+  return {
+    handle: {} as any,
+    llm,
+    user: {} as any,
+    agent: {} as any,
+    model: GENERATOR_MODEL as any,
+    judgeModel: judgeModel as any,
+    sessionID: "ses_test",
+    system: [],
+    messages: [],
+    tools: {},
+  }
+}
+
+const candidates = [
+  { index: 0, reasoning: "", text: "a", toolCalls: [], finishReason: "stop" },
+  { index: 1, reasoning: "", text: "b", toolCalls: [], finishReason: "stop" },
+] as unknown as Candidate[]
+
+describe("max-mode judge model (blind verification)", () => {
+  test("judge uses the separate judgeModel when configured", async () => {
+    const { llm, models } = captureLLM()
+    const result = await Effect.runPromise(judge(input(llm, JUDGE_MODEL), candidates))
+    expect(result.pick).toBe(1)
+    expect(models).toHaveLength(1)
+    expect(models[0]).toBe(JUDGE_MODEL)
+  })
+
+  test("judge falls back to the step model when judgeModel unset", async () => {
+    const { llm, models } = captureLLM()
+    await Effect.runPromise(judge(input(llm), candidates))
+    expect(models).toHaveLength(1)
+    expect(models[0]).toBe(GENERATOR_MODEL)
+  })
+
+  test("single candidate short-circuits without any judge call (either model)", async () => {
+    const { llm, models } = captureLLM()
+    const result = await Effect.runPromise(judge(input(llm, JUDGE_MODEL), candidates.slice(0, 1)))
+    expect(result.pick).toBe(0)
+    expect(models).toHaveLength(0)
+  })
+})
